# Simulator binaries (Windows-only, too large for git)
_backup

# Original archives
*.zip

# Python
__pycache__/
*.pyc
.venv/

# ML artifacts — kept on disk locally, but too large / regenerable for git
yolo*.pt
rl/data/gate_pose_ds/
rl/data/gate_pose.pt
